/* Hand-written declaration for the prebuilt scripts.bundle.js (compiled Lua
   card-script bundle, ~2.3MB — not something TS should parse for types). */
export const SCRIPTS: unknown
export function scriptReader(name: string): string
