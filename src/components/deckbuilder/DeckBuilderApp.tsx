// Deck builder root component. Scaffold checkpoint — ported from
// isola/engine/deckbuilder/app.js in a later phase.
function DeckBuilderApp() {
  return (
    <div id="boot">
      <p>Goat Format Deck Builder — scaffold checkpoint.</p>
    </div>
  )
}

export default DeckBuilderApp
